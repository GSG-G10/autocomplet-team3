const homeHandler = require('./homeHandler');
const searchHandler = require('./searchHandler');
const handleStaticFile = require('./staticFile');

module.exports = {
  homeHandler,
  searchHandler,
  handleStaticFile,
};
